app = "llm-council-a3ixvg"
primary_region = "nrt"

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[env]
  PORT = "8080"

[mounts]
  source = "llm_council_data"
  destination = "/app/data"
